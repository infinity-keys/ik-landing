{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web",
      "packages/*"
    ]
  },
  "scripts": {
    "build": "yarn workspaces foreach run build & yarn rw exec generateAnonPuzzleData",
    "dev": "(yarn workspaces foreach run build-dev & yarn workspace @infinity-keys/react-lens-share-button build & yarn rw exec generateAnonPuzzleData) && rw dev",
    "seed": "rw prisma migrate reset -f",
    "cypress:open": "start-server-and-test dev http://localhost:8910 \"yarn workspace cypress cypress:open\"",
    "cypress:run": "yarn workspace cypress cypress run --headless --browser chrome"
  },
  "devDependencies": {
    "@redwoodjs/core": "^3.4.0",
    "start-server-and-test": "^2.0.0",
    "zod": "^3.19.1"
  },
  "eslintConfig": {
    "extends": "@redwoodjs/eslint-config",
    "root": true
  },
  "engines": {
    "node": ">=14.19 <=16.x",
    "yarn": ">=1.15"
  },
  "prisma": {
    "seed": "yarn rw exec seed"
  },
  "packageManager": "yarn@3.2.3",
  "dependencies": {
    "lodash": "^4.17.21",
    "node-fetch": "^2"
  }
}
