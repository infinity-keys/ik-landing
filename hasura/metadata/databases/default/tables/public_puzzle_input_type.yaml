table:
  name: puzzle_input_type
  schema: public
is_enum: true
array_relationships:
  - name: puzzles
    using:
      foreign_key_constraint_on:
        column: input_type
        table:
          name: puzzles
          schema: public
select_permissions:
  - role: api
    permission:
      columns:
        - type
      filter: {}
